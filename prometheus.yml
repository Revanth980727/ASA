global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # ASA Backend API
  - job_name: 'asa-backend'
    static_configs:
      - targets: ['host.docker.internal:8000']  # Use host.docker.internal to reach host machine
    metrics_path: '/metrics'
    scrape_interval: 10s
