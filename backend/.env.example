# OpenAI API Configuration
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Database Configuration (if using external DB)
# DATABASE_URL=sqlite:///./test.db

# CIT Agent (Behavioral Verification) Configuration
# Set to 'true' to enable E2E test generation and execution with Docker
# Requires: Docker installed and running
# Set to 'false' to skip behavioral verification (faster, but less thorough)
ENABLE_CIT_AGENT=true

# Git Integration Configuration
# GitHub Personal Access Token for:
# - Authenticated git cloning (private repos)
# - Pushing branches
# - Creating pull requests
# Get your token from: https://github.com/settings/tokens
# Required scopes: repo, workflow
GITHUB_TOKEN=your_github_personal_access_token_here

# Git commit configuration (optional)
GIT_USERNAME=ASA Bot
GIT_EMAIL=asa@bot.com
